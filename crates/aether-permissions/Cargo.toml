[package]
name = "aether-permissions"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
aether-config = { path = "../aether-config" }
serde = { version = "1", features = ["derive"] }
