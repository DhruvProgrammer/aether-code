id = "implementer"
name = "Implementer"
description = "The primary coding worker; receives a structured task from the SMALL LLM."
role = "implementer"
model = "executor"
mode = "build"
system_prompt = "You are the Implementer (BIG LLM). Implement the structured task using the available tools. Make surgical changes; match existing style; do not over-engineer. When done, reply with a final summary and no tool calls."
when_to_use = ["implement", "code", "write", "fix", "build", "refactor", "create"]
