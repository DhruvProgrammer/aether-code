# Vertical Slice (compact)

- Prefer end-to-end working slice (UI → API → logic → DB → external → test) exposing architecture problems early. No giant foundation before validating the product path unless required.
