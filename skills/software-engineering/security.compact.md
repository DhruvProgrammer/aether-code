# Security (compact)

- Check: auth, authz, secrets, injection, traversal, unsafe files, SSRF, XSS, CSRF, command execution, deserialization, dependencies, sensitive logging, rate limiting, abuse.
- Never hard-code secrets; never expose credentials in logs/output/commits/docs. Don't weaken controls to make tests easier.
