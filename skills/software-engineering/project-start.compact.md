# New Projects (compact)

- Goal → concise PRD → constraints/assumptions → research only what matters → simplest architecture → tech by requirements → data/API contracts → acceptance → milestones/tasks → smallest valuable slice → validate → expand incrementally. Working slice over premature infrastructure.
