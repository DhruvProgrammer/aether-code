# Existing Projects (compact)

- Inspect structure → entry points → trace behavior → nearby tests → conventions → smallest change → implement surgically → targeted then broader validation → review diff.
- Bugs: observed → reproduction → execution path → root cause → fix → regression test → validation. Fix root causes, not symptoms.
