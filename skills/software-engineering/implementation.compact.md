# Implementation Workflow (compact)

- Per task: Inspect → Reproduce/baseline (bugs) → Plan (files, risks, validation) → Implement smallest coherent change → Validate narrowly first (focused test), then broader → Review diff (correctness, edges, security, compat) → Update docs.
