# Refactoring (compact)

- Refactor only when it enables the change, fixes a relevant structural problem, materially improves correctness/maintainability, or is requested. Preserve behavior; separate unrelated refactors; validate incrementally. No cleanup projects disguised as features.
